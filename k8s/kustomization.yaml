apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app: hub-be
  managed-by: kustomize

# Optional: Add common annotations
# commonAnnotations:
#   environment: production

# Optional: Image transformer (if you want to change image tags)
# images:
#   - name: hub-be
#     newTag: v1.0.0
